// Backend API address. This is the ONLY file you should need to edit before
// publishing this folder to GitHub Pages — point it at wherever the Gin
// backend (bedah-buku, port 5555 by default) is reachable from visitors.
//
// Example once the backend has a public/tunnelled address:
//   const API_BASE = "https://your-backend-domain.example.com";
const API_BASE = "https://outbid-earthy-freeware.ngrok-free.dev";
